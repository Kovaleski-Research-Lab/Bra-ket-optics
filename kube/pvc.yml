apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: braket-results
  namespace: gpn-mizzou-muem
spec:
  storageClassName: rook-cephfs-central
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi

